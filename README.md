# Nav React

A simple React app to practice React Router DOM with dynamic routing and navigation.

## About
Built to learn client-side routing in React using React Router DOM.
Includes dynamic URL params to render pages.

## Features
- Multi-page navigation (Home, About, Services, Contact)
- Dynamic route with useParams
- page Not Found for unknown routes
